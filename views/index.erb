<% if session[:message] %>
  <p><%= session.delete(:message) %></p>
<% end %>

<h3><%= root %></h3>
<ul>
  <% @files.each do |file| %>
    <li>
      <a href="/<%= file %>"><%= file %></a>
    </li>
  <% end %>
</ul>
